En la carpeta "Config", agregar los dos siguientes archivos:

config.py:  
TOKEN = 'Tu Token'
API_KEY_WEATHER = 'Tu Api de Weather'


config.json: 
{
    "token": "Tu Token aquí (remplazar con el token real)",
    "prefix": "Prefijo para comandos (por ejemplo, '!')"
  }
  
